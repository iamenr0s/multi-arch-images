name: kibana

on:
  push:
    paths:
      - '.github/workflows/kibana.yml'
      - 'docker_builds/kibana/**'
      - '!docker_builds/kibana/*.md'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        kibana-version: [ 7.9.3 ]
    env:
      VERSION: $(cat docker_builds/kibana/.version)
    steps:
      - 
        name: Checkout
        uses: actions/checkout@v2
      - 
        name: Set up Docker Buildx
        uses: crazy-max/ghaction-docker-buildx@v3
        with:
          buildx-version: latest
      -
        name: Cache Docker layers
        uses: actions/cache@v2
        id: cache
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ matrix.kibana-version }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-
      - 
        name: Docker Buildx (build)
        run: |
          docker buildx build \
            --cache-from "type=local,src=/tmp/.buildx-cache" \
            --cache-to "type=local,dest=/tmp/.buildx-cache" \
            --platform linux/amd64,linux/arm64 \
            --output "type=image,push=false" \
            --build-arg KIBANA_VERSION=${{ matrix.kibana-version }} \
            --tag enros/kibana:${{ matrix.kibana-version }} \
            --file ./docker_builds/kibana/Dockerfile \
            ./docker_builds/kibana/
      -        
        name: Login to GitHub Docker Registry
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}        
        run: |
          echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin
      -  
        name: Docker Buildx (push)
        run: |
          docker buildx build \
            --cache-from "type=local,src=/tmp/.buildx-cache" \
            --platform linux/amd64,linux/arm64 \
            --output "type=image,push=true" \
            --build-arg KIBANA_VERSION=${{ matrix.kibana-version }} \
            --tag enros/kibana:${{ matrix.kibana-version }} \
            --file ./docker_builds/kibana/Dockerfile \
            ./docker_builds/kibana/
      -
        name: Docker Check Manifest
        run: |
          docker run --rm mplatform/mquery:latest enros/kibana:${{ matrix.kibana-version }}
      -
        name: Clear
        if: always()
        run: |
          rm -f ${HOME}/.docker/config.json
